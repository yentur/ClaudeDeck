## What does this change?

<!-- A short description of the change and why it's needed. Link related issues, e.g. "Fixes #12". -->

## How was it tested?

<!-- e.g. swift test, fake-session sandbox (scripts/fake-sessions.py + scripts/dev-run.sh), which terminal apps, light/dark. -->

## Screenshots

<!-- For UI changes. Please use the fake-session sandbox, not real sessions. -->

## Checklist

- [ ] `swift build` has no new warnings and `swift test` passes
- [ ] New logic lives in `DeckCore` with tests (or I explained why not)
- [ ] New user-visible strings are in `Strings.swift` in English and Turkish
- [ ] No network requests, no writes under `~/.claude`, no personal data in code, fixtures or screenshots
- [ ] `CHANGELOG.md` updated under `## [Unreleased]` for user-visible changes
